<%= form_with(model: griffin_cc, local: true) do |form| %>
  <% if griffin_cc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(griffin_cc.errors.count, "error") %> prohibited this griffin_cc from being saved:</h2>

      <ul>
        <% griffin_cc.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
